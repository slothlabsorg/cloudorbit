# Homebrew Cask — CloudOrbit
# Auto-generated by .github/workflows/update-tap.yml on every release publish.
# Template tokens: __VERSION__  __SHA_ARM__  __SHA_X64__
#
# Manual install: brew install --cask slothlabsorg/tap/cloudorbit
# Tap setup:      brew tap slothlabsorg/tap

cask "cloudorbit" do
  version "__VERSION__"

  on_arm do
    url "https://github.com/slothlabsorg/cloudorbit/releases/download/v#{version}/CloudOrbit_#{version}_aarch64.dmg"
    sha256 "__SHA_ARM__"
  end

  on_intel do
    url "https://github.com/slothlabsorg/cloudorbit/releases/download/v#{version}/CloudOrbit_#{version}_x64.dmg"
    sha256 "__SHA_X64__"
  end

  name        "CloudOrbit"
  desc        "AWS session manager — SSO, kubeconfig, web console in one app"
  homepage    "https://github.com/slothlabsorg/cloudorbit"

  app "CloudOrbit.app"

  zap trash: [
    "~/Library/Application Support/io.slothlabs.cloudorbit",
    "~/Library/Logs/io.slothlabs.cloudorbit",
    "~/Library/Preferences/io.slothlabs.cloudorbit.plist",
    "~/Library/Saved Application State/io.slothlabs.cloudorbit.savedState",
  ]
end
