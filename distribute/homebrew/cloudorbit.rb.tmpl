# Homebrew Cask — CloudOrbit
# This file is auto-generated by .github/workflows/update-tap.yml
# Template tokens: __VERSION__  __SHA_ARM__  __SHA_X64__
#
# Manual install: brew install --cask slothlabs/tap/cloudorbit
# Tap setup:      brew tap slothlabs/tap

cask "cloudorbit" do
  version "__VERSION__"

  on_arm do
    url "https://github.com/slothlabs/cloudorbit/releases/download/v#{version}/CloudOrbit_#{version}_aarch64.dmg"
    sha256 "__SHA_ARM__"
  end

  on_intel do
    url "https://github.com/slothlabs/cloudorbit/releases/download/v#{version}/CloudOrbit_#{version}_x64.dmg"
    sha256 "__SHA_X64__"
  end

  name "CloudOrbit"
  desc "AWS session manager — SSO, kubeconfig, web console in one app"
  homepage "https://slothlabs.org/cloudorbit"

  app "CloudOrbit.app"

  # Remove all app data when the user runs: brew uninstall --zap cloudorbit
  zap trash: [
    "~/Library/Application Support/io.slothlabs.cloudorbit",
    "~/Library/Logs/io.slothlabs.cloudorbit",
    "~/Library/Preferences/io.slothlabs.cloudorbit.plist",
    "~/Library/Saved Application State/io.slothlabs.cloudorbit.savedState",
  ]
end
